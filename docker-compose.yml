services:
  product-service:
    env_file:
      - .env
    build:
      context: .
      dockerfile: cmd/productapp/Dockerfile
    container_name: OrderServiceApp
    ports:
      - "50053:50053"
    depends_on:
      - db-service-product

  db-service-product:
    image: postgres:18.3-bookworm
    container_name: ProductDataBase
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ${PROJECT_ROOT}/out/pgdata:/var/lib/postgresql
    restart: always

  migrate:
    image: migrate/migrate:v4.19.1
    container_name: migrate-container
    volumes:
      - ${PROJECT_ROOT}/migrations:/migrations

  forwarder-port:
    image: alpine/socat:1.8.0.3
    container_name: product-service-postgres-forwarder-port
    command: "tcp-listen:5432,fork,reuseaddr tcp-connect:db-service-order:5432"
    ports:
      - "127.0.0.1:5432:5432"